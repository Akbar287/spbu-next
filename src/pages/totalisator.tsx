import React from "react";
import Layout from "@/components/Layout";

const Totalisator = () => {
    return (
        <Layout title="Totalisator" isReady={true}>
            <h1>Totalisator</h1>
        </Layout>
    );
};

export default Totalisator;
