import Layout from "@/components/Layout";
import React from "react";

const About = () => {
    return (
        <Layout title="About" isReady>
            p
        </Layout>
    );
};

export default About;
