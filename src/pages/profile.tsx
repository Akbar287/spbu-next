import Layout from "@/components/Layout";
import React from "react";

const Profile = () => {
    return (
        <Layout title="Profile" isReady={true}>
            <h1>Profile</h1>
        </Layout>
    );
};

export default Profile;
