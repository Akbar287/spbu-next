import React from "react";
import Layout from "./Layout";

const Dashboard = () => {
    return (
        <Layout title="Dashboard" isReady={true}>
            <h1>Dashboard</h1>
        </Layout>
    );
};

export default Dashboard;
